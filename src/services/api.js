const api = {
  get: async (url) => {
    // Placeholder for API call
    return { data: [], status: 200 };
  },
  post: async (url, data) => {
    // Placeholder for API call
    return { data: { success: true }, status: 201 };
  },
};

export default api;